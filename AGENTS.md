# AGENTS.md — laya 项目全局开发规则

> 本文件是所有在 laya 项目中写代码、跑自动化、管服务的 Agent / 人都必须遵守的硬性规则。
> 规则来源：线上踩坑血泪教训汇总。任何一条与"快速验证一下"冲突时，**以本文件为准**。

---

## 1. 项目一句话

laya = 一套多模型浏览器自动化 + FastAPI 封装的服务集群：
用 Playwright / 受控 Chrome 驱动各家网页版大模型，把它们包成本地 HTTP 服务，
每个模型一个 Chrome profile、一个端口、一个 launchd 常驻。

---

## 2. 风控与拟人化操作（最高优先级，血泪教训）

> 千问因为短时间内反复问同一个问题，直接触发人机验证，解不开，停摆数小时。
> 这一节是**血的教训**，新模型接入时**必须**逐条落地，不许偷懒。

### 2.1 提问节奏（反风控核心）
- **禁止**短时间内反复问同一个问题——千问就是这么挂的。
- 每次测试必须换不同问题，**不重复**。
- 任意两次提问之间间隔 **≥ 30 秒**；批量测试要严格控制频率，宁可慢不要猛。
- 同一轮测试用例不要堆得太长，发现连续 2~3 次异常就立即收手，不要硬跑。

### 2.2 拟人化输入动作
- **逐字输入**，每个字之间 **50~150ms 随机延迟**，不要 `fill()` 一把灌进去。
- 操作前（点击按钮、聚焦输入框、提交）随机停顿 **0.5~2 秒**。
- 输入前**先点击输入框聚焦**，再开始打字；不要在未聚焦状态下直接 send_keys。
- 鼠标移动、点击位置带一点随机偏移，不要每次都精确命中正中心。

### 2.3 浏览器反检测
- 隐藏 `navigator.webdriver`（置 undefined）。
- 伪造 `navigator.plugins` / `navigator.languages` / `navigator.chrome`，让 headless / 受控态看起来像真人浏览器。
- 启动参数里带常见的真人 flags，不要用裸 `--headless`。
- 每个模型用独立 user-data-dir（见 §3.4），不要共用 profile。

### 2.4 触发风控后的处理（唯一正确姿势）
1. **立即停止**一切自动化操作，不要再点、不要再问。
2. **静置冷却 ≥ 30 分钟**，期间不要再尝试自动化绕过。
3. 用**真人手动**过一次人机验证 / 验证码 / 滑块。
4. **不要**强行重试、不要换 IP 猛刷、不要写脚本自动点验证——只会越陷越深。
5. 冷却后，先用 1 个全新的、简单的问题手工验证恢复，再恢复自动化。

---

## 3. 多模型架构规范

### 3.1 公共层 `core/`
所有模型共用的能力只写一份，放在 `core/`：
- `core/browser_base.py`：浏览器基类（启动、反检测、拟人化输入、登录态保持、优雅关闭）。
- `core/server_base.py`：FastAPI 工厂（统一路由、shutdown 钩子、日志、错误处理）。

**禁止**在每个 bot 里重复造浏览器启动 / FastAPI 脚手架。

### 3.2 模型适配层 `bots/`
每个模型在 `bots/` 下写一个适配文件，**只**实现以下 6 个方法 / 属性，其它一律复用 core：
- `new_chat()`：点击"新对话"按钮，清空上下文。
- `is_logged_in()`：判断当前 profile 是否已登录。
- `extract_answer()`：从页面抽取最新一条回答正文。
- `extract_citations()`：抽取引用 / 来源列表。
- `stop_button_selector`：停止生成按钮的选择器（用于中断）。
- `maybe_enable_search()`：是否需要在发送前打开"联网搜索"开关（按模型能力决定）。

### 3.3 入口脚本 `run_xxx.py`
每个模型一个**薄封装**入口，只做：
- import 对应 bot 适配类；
- 从 `core/server_base` 起 FastAPI；
- 监听指定端口。

端口分配（递增，不要乱跳）：

| 模型 | 端口 |
| --- | --- |
| DeepSeek | 8000 |
| 千问 | 8001 |
| 后续新模型 | 8002、8003…… |

### 3.4 浏览器 profile
- 每个模型**一个独立 Chrome profile**，统一放在 `profiles/` 下。
- profile 目录已在 `.gitignore` 中排除，**绝对不要** commit、不要清理、不要复用给别的模型。

### 3.5 launchd 常驻
- 每个模型一个 launchd plist，命名 `com.laya.<模型名>.plist`。
- 配置 `KeepAlive=true` + `RunAtLoad=true`，崩溃自动拉起、开机自启。

---

## 4. 已知模型选择器表

> 选择器会随前端改版漂移。修改前先在浏览器 DevTools 里复核；确认后回填到这里。

### 4.1 DeepSeek（稳定运行中）
- 输入框：`textarea`
- 新对话按钮：`text=开启新对话`
- 回答正文：`.ds-assistant-message-main-content`
- 引用：`.ds-markdown-cite` 标记 + 外链

### 4.2 千问（风控暂停中）
- 输入框：`[contenteditable='true']`
- 新对话按钮：`text=新建对话`
- 回答：`[class*='markdown']` 最后一个
- 来源统计：按"篇来源"计数；完整列表选择器待风控解除后补全。

---

## 5. 服务管理

### 5.1 launchctl 常用命令
- 启动：`launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.laya.<模型>.plist`
- 停止：`launchctl bootout gui/$(id -u)/com.laya.<模型>`
- 查看：`launchctl list | grep laya`

### 5.2 优雅关闭
- 服务收到 FastAPI `shutdown` 事件时，**自动关闭浏览器**（`context.close()` / `browser.close()`）。
- **禁止** `kill -9` 直接杀进程——会污染 Chrome profile、留下锁文件、下次启动报 user-data-dir 被占用。
- 真的卡住了，先 `kill` 正常信号，等 5 秒再考虑 `-9`，并同步清理 profile 下的 `SingletonLock`。

### 5.3 日志
- 所有服务日志写到 `server_logs/` 下，**按服务名分文件**（如 `deepseek.log`、`qianwen.log`）。
- 日志目录已在 `.gitignore` 中排除。

---

## 6. 当前状态（截至本次 init）

- **DeepSeek**：稳定运行中，端口 8000。
- **千问**：因触发风控暂停，端口 8001；**过几小时再继续**，本轮及临近时段**不要对千问做任何自动化测试**。恢复前先手动登录、手动验证一次。
- **laya-model/**：备用本地模型目录，**暂不调用**，仅保留代码。

---

## 7. 仓库与提交纪律（硬性约束）

以下目录**只在本机跑，绝不入库、绝不删除、绝不修改内容**：
- `server_logs/`（运行日志）
- `profiles/`（浏览器 profile / 登录态）
- `.venv39/`（Python 虚拟环境）
- `archive/`（历史归档）

`.gitignore` 已经把这些排除掉了。`git status` 出现这些路径的变更 = 哪里出错了，立刻停下来排查，不要硬提交。

提交信息风格：前缀简短动作 + 一句说明，例如：
- `init: 初始化项目与全局开发规则`
- `feat(bots/qianwen): 补全来源选择器`
- `fix(core): 修复 shutdown 时浏览器未关闭`

---

## 8. 给下一个接手机器人 / 人的 checklist

接入一个新模型时，按顺序走：
1. 先把 §2 风控规则在新 bot 里逐条落地（拟人化输入 + 反检测 + 间隔）。
2. 在 `bots/` 写适配类，只实现 §3.2 列的 6 项，其它一律复用 `core/`。
3. 新建独立 `profiles/<模型>/`，分配下一个端口（8002 起）。
4. 写 `run_<模型>.py` 薄封装 + `com.laya.<模型>.plist`。
5. 第一次只手工跑 1~2 个**不同的**问题验证，间隔 ≥ 30 秒；稳定了再挂自动化。
6. 选择器确认后回填到 §4。
