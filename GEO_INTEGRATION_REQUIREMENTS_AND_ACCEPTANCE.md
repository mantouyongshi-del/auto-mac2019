# 农场系统 GEO 接入要求、开发规范与验收标准

版本：v1.0
日期：2026-09-23
适用项目：`~/laya` 农场系统
目标接入方：GEO Inspector / 部门业务系统

## 1. 文档目标

农场长期运行浏览器模型，持续采集真实用户视角下的回答正文、引用链接、搜索词和运行状态，作为 GEO 报告和相关分析功能的外部观测数据源。

农场不负责品牌评分、报告结论或业务数据库写入。农场只负责：问题调度、浏览器执行、原始结果保存、任务状态管理和结果交付。

## 2. 当前架构约束

- 一个农场实例专门服务一个部门。
- 当前模型服务和 Dashboard 绑定本机地址，不能直接暴露公网。
- 模型回答耗时较长，十道问题可能需要十分钟以上。
- 浏览器平台存在反自动化风控，问题必须去重并设置冷却间隔。
- 单个浏览器实例同一时间只能执行一个问题。
- 原始回答和引用是审计证据，不能被清洗逻辑覆盖。

## 3. 接入原则

1. 使用异步任务，不使用长时间同步 HTTP 请求。
2. 逐题落库，支持部分完成和断点恢复。
3. 原始数据与解析、评分结果分离保存。
4. 所有任务和结果必须幂等，重试不能产生重复样本。
5. 通过专用 GEO Gateway 接入，不直接开放 Dashboard 管理接口。
6. 默认走 SSH 隧道或内网专线，不把模型端口暴露到公网。
7. API 结果和浏览器农场结果分开标识，不直接混合计算。
8. 遵守模型平台服务条款和农场既有的最短间隔、每日上限及休息策略。

## 4. P0 功能要求

### 4.1 专用 GEO Gateway

新增独立的业务接口层，至少提供：

- `POST /geo/tasks`：创建 GEO 采样任务
- `GET /geo/tasks/{task_id}`：查询任务摘要和进度
- `GET /geo/tasks/{task_id}/results`：分页读取逐题结果
- `POST /geo/tasks/{task_id}/cancel`：取消尚未执行的问题
- `GET /geo/health`：返回服务、浏览器、登录态和队列健康状态

Gateway 不应复用 Dashboard 的网页登录 Cookie，也不应把 Dashboard 密码写入调用方配置。

### 4.2 鉴权

- 所有业务接口必须校验 `X-API-Key` 或等价的服务令牌。
- 令牌从环境变量读取，缺失时服务必须拒绝启动或拒绝业务请求，不能默认放行。
- Dashboard 管理密码从环境变量读取，禁止硬编码。
- 日志、任务文件、错误响应不得输出令牌、Cookie、登录资料或浏览器 profile 路径。
- 令牌轮换通过环境变量和重启完成，不在代码或提交记录中保存真实值。

### 4.3 任务创建协议

请求示例：

```json
{
  "department_id": "dept-example",
  "brand_code": "brand-example",
  "questions": [
    {"question_id": "q-001", "text": "示例问题"}
  ],
  "models": ["deepseek"],
  "sampling_window": "2026-09-23T00:00:00+08:00/2026-09-24T00:00:00+08:00",
  "priority": "normal",
  "metadata": {"source": "geo-inspector"}
}
```

响应必须在短时间内返回，不等待模型回答：

```json
{
  "task_id": "task_...",
  "status": "queued",
  "total": 1,
  "completed": 0,
  "failed": 0
}
```

### 4.4 任务状态

任务状态限定为：

`queued`、`running`、`partial`、`completed`、`failed`、`cancelled`。

任务必须记录：

- `task_id`
- `department_id`
- `brand_code`
- `created_at`、`started_at`、`finished_at`
- `total`、`completed`、`failed`、`cancelled`
- 选用的模型列表
- 调度参数和失败摘要
- 农场版本、浏览器版本、提示词版本

### 4.5 逐题结果协议

每道题完成后立即保存并可读取：

```json
{
  "task_id": "task_...",
  "question_id": "q-001",
  "question": "示例问题",
  "model": "deepseek",
  "status": "success",
  "answer": "原始回答正文",
  "citations": [
    {"ref": "1", "url": "https://example.com", "title": "标题"}
  ],
  "search_queries": ["示例搜索词"],
  "requested_at": "...",
  "started_at": "...",
  "finished_at": "...",
  "source": "laya-browser",
  "farm_version": "...",
  "prompt_version": "...",
  "error": null
}
```

失败结果也必须保存，包含可读错误分类：`timeout`、`not_logged_in`、`rate_limited`、`browser_error`、`service_unavailable`、`unknown`。

## 5. 幂等与数据要求

### 5.1 唯一键

结果唯一键建议为：

`task_id + question_id + model + sampling_window`

重复提交同一任务不得重复执行；网络重试应返回原任务状态。

### 5.2 原始证据

- `answer`、`citations`、`search_queries` 保存原始值。
- 清洗、品牌提及判断、情感判断和位置提取另存派生字段。
- 不得用派生结果覆盖原始回答。
- 每条结果保留来源标识 `source=laya-browser`。

### 5.3 时间与版本

所有时间使用 ISO 8601，并明确时区。每条结果至少带有：

- 农场代码版本
- 浏览器版本
- 模型服务版本
- 提示词版本
- 采样时间窗口

## 6. 调度、风控与恢复

- 默认沿用当前 15–25 秒随机间隔，最小值和最大值必须由环境变量配置。
- 对高风险模型、出现风控信号或连续失败时，自动切换到 30 秒以上的保守退避策略。
- 调用方不能绕过农场配置的最小安全间隔。
- 同一问题和同一模型在冷却窗口内不得重复执行。
- 遇到风控或登录失效时进入退避，不得立即高频重试。
- 任务支持暂停、取消和恢复。
- 进程重启后从 `queued` 或未完成的题目继续。
- 单题失败不应导致整份任务丢失。
- 每个模型实例保持串行，队列层负责跨模型调度。
- 任务执行时间不得由 HTTP 请求超时限制，调用方通过轮询或事件获取进度。

## 7. 健康检查与监控

健康接口至少区分：

- `process`: 进程是否存活
- `browser`: 浏览器是否可用
- `login`: 各模型是否已登录
- `queue`: 队列长度和运行任务数
- `last_success_at`: 各模型最近一次成功时间
- `last_error`: 最近一次错误分类和时间
- `disk`: 任务、日志和 profile 所在磁盘使用率

监控必须能发现“HTTP 在线但浏览器不可用”“进程在线但已退出登录”“任务长期无进展”等情况。

## 8. 日志与数据保留

- 请求日志、任务状态和原始结果分开存储。
- 日志采用 JSONL 或等价结构化格式。
- 日志中不得出现 API Key、Cookie、密码和完整 profile 内容。
- 任务文件必须有保留、归档和导出策略。
- 磁盘达到阈值时告警，不得自动删除未归档的原始证据。
- 所有取消、失败、重试和恢复动作记录审计事件。

## 9. GEO 系统对接约定

GEO Inspector 侧只依赖 Gateway，不直接访问浏览器模型端口。接入时：

1. 创建异步任务并保存 `task_id`。
2. 定期查询任务摘要。
3. 按题读取已完成结果。
4. 将原始正文、引用和搜索词保存为外部观测证据。
5. 全部完成后再生成汇总报告。
6. 未完成报告标记为 `partial`，不能当作完整报告。

浏览器农场样本与官方 API 样本必须分别标记来源、采样条件和时间，只有满足相同条件时才允许进行直接对比。

## 10. 实施顺序

### 阶段一：安全与协议

- 实现专用 GEO Gateway
- 强制 API Key
- 移除硬编码 Dashboard 密码
- 完成任务和逐题结果 JSON 协议

### 阶段二：可靠执行

- 幂等键
- 逐题落库
- 任务进度查询
- 失败分类、重试和退避
- 重启恢复

### 阶段三：运维能力

- 健康检查细化
- 队列和延迟监控
- 磁盘与日志保留
- 版本和采样窗口记录

### 阶段四：隔离联调

- 只使用一个模型
- 只使用一个新问题
- 只跑一份测试任务
- 通过 SSH 隧道或内网地址联调
- 验证成功后再扩大模型和问题数量

## 11. 验收标准

### A. 安全验收

- [ ] 未携带服务令牌访问业务接口返回 401/403。
- [ ] 错误令牌访问业务接口返回 401/403。
- [ ] 未配置令牌时，业务接口不会默认放行。
- [ ] 代码和日志中不存在真实密码、Cookie、API Key。
- [ ] Dashboard 密码不再硬编码。
- [ ] 模型端口未暴露到公网。

### B. 任务验收

- [ ] 创建任务在 5 秒内返回 `task_id`，不等待模型完成。
- [ ] 10 道问题任务可以持续运行 10 分钟以上而不触发 HTTP 超时。
- [ ] 任务摘要能显示总数、完成数、失败数和当前状态。
- [ ] 单题完成后 30 秒内可通过结果接口读取。
- [ ] 任务支持取消，取消后未开始的问题不会继续执行。
- [ ] 进程重启后能恢复未完成任务。

### C. 数据验收

- [ ] 每条结果包含正文、引用、搜索词、模型、时间和来源。
- [ ] 失败题目保留失败分类和错误信息。
- [ ] 重试不会产生重复结果。
- [ ] 原始证据和派生判断分开存储。
- [ ] 任务、问题、品牌和部门标识完整。

### D. 风控验收

- [ ] 同一模型连续问题间隔符合环境变量配置，默认沿用 15–25 秒随机间隔。
- [ ] 出现风控、验证码或连续失败时，系统会自动进入 30 秒以上的保守退避。
- [ ] 相同问题在冷却窗口内不会重复提交。
- [ ] 风控、未登录和浏览器异常会进入退避，而不是高频重试。
- [ ] 单个模型异常不会阻塞其他模型的任务状态记录。

### E. 运维验收

- [ ] 健康接口能区分进程、浏览器和登录态。
- [ ] 能查询最近成功时间、最近错误和队列长度。
- [ ] 磁盘接近阈值时产生告警。
- [ ] 任务和审计日志可以按任务导出。
- [ ] 日志不泄露凭据和浏览器登录资料。

### F. 联调验收

- [ ] 通过 SSH 隧道完成单模型、单问题测试。
- [ ] GEO Inspector 能创建任务并读取逐题结果。
- [ ] GEO 报告显示样本来源为 `laya-browser`。
- [ ] 未完成任务显示 `partial`，不会被误判为完整报告。
- [ ] 测试任务的原始回答、引用和搜索词可追溯到 `task_id`。

## 12. 不在本阶段范围内

- 不直接把农场结果写入 GEO 业务数据库。
- 不把 Dashboard 改造成公开互联网服务。
- 不同时支持复杂多租户调度。
- 不自动轮换外部平台账号密钥。
- 不以一次成功的浏览器回答替代长期样本验证。

## 13. 交付物

完成阶段一和阶段二后，农场项目至少应交付：

1. GEO Gateway 服务代码。
2. 环境变量示例文件，不包含真实密钥。
3. API 协议示例和错误码说明。
4. 任务状态与结果存储实现。
5. 重试、恢复和幂等测试记录。
6. 单模型单问题联调记录。
7. 健康检查和运维说明。
